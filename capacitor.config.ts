import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.primetv.iptv',
  appName: 'PrimeTV',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
